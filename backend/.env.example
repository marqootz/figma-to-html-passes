# Backend Server Configuration
# Copy this file to .env and fill in your values

# Google OAuth (PKCE-only: only Client ID required)
# Create a "Desktop app" OAuth client at https://console.cloud.google.com/apis/credentials
# Add redirect URI: http://localhost:3000/api/google-drive/oauth/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=optional-for-Web-application-client-only

# Server Configuration
PORT=3000

# Callback URL (optional - defaults to http://localhost:PORT/api/google-drive/oauth/callback)
# For production, set this to your public URL
# CALLBACK_URL=https://your-domain.com/api/google-drive/oauth/callback

# Optional: used by plugin build (npm run build) when building for distribution.
# If set here, the plugin will have these values built in; no need to export them.
# GOOGLE_AUTH_BACKEND_URL=http://localhost:3000
# GOOGLE_DRIVE_FOLDER_ID=your-presentations-folder-id-from-drive-url
